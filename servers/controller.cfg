define host {
        use                     linux-server
        host_name               controller
        alias                   controller
        address                 10.1.20.4
}

define service {
        use                     generic-service
        host_name               controller
        service_description     PING
        check_command           check_ping!100.0,20%!500.0,60%
}